spring:
  profiles: dev
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://www.leleplus.fun:3306/driver-dev?
    username: lele
    password: lele
#  jackson:
#    serialization: true  # JSON格式化

mybatis:
  mapper-locations: classpath:mapper/**/*.xml

#driver:
#  token:
#    secret: auth
#    header: token
#    expiration-ime: 3600
#    expiration-time-remember: 86400

logging:
  level:
    org.springframework.security.oauth2: DEBUG
    org.apache.ibatis.mapping: DEBUG
  #    com.system.persistence.read.QuestionReadMapper: DEBUG
  #    org.springframework: DEBUG
  config: classpath:logback-spring.xml
  file: logs/system-server-dev.log